OUR LITTLE ADVENTURES — SHARED VERSION

1. Open SETUP.sql and run it in Supabase SQL Editor.
2. Open index.html to test the login.
3. IMPORTANT: This package contains the publishable Supabase key only. Never put a service-role/secret key in frontend code.

The database schema includes a shared couple system. The next UI step is to add the couple-code onboarding screen so both accounts can join the same couple automatically.
